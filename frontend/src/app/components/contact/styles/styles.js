import {makeStyles} from '@mui/styles';

export const useStyles = makeStyles({
    contactContainer:{
        display: 'flex',
        alignItems: 'center',
        padding: '0.5em'
    },
    contactName:{
        marginLeft: '0.5em',
    }
})