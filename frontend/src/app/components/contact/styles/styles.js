import {makeStyles} from '@mui/styles';

export const useStyles = makeStyles({
    contactContainer:{
        display: 'flex',
        alignItems: 'center',
        padding: '0.5em',
        cursor: 'pointer',
        marginLeft: '0.5em!important',

    },
    contactName:{
        marginLeft: '0.5em!important',
    }
})