import {makeStyles} from '@mui/styles';

export const useStyles = makeStyles({
    contactContainer:{
        display: 'flex',
        alignItems: 'center',
        padding: '0.5em',
        cursor: 'pointer'
    },
    contactName:{
        marginLeft: '0.5em!important',
    }
})