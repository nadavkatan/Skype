import {makeStyles} from '@mui/styles';

export const useStyles = makeStyles({
    protectedScreenSpinnerContainer:{
        display: 'flex', 
        justifyContent: 'center', 
        alignItems: 'center', 
        height: '100vh',
         width: '100vw'
    }
})